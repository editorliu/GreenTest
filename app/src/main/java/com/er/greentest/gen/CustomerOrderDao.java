package com.er.greentest.gen;

import java.util.List;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;
import org.greenrobot.greendao.query.Query;
import org.greenrobot.greendao.query.QueryBuilder;

import com.er.greentest.entity.tomany.CustomerOrder;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "CUSTOMER_ORDER".
*/
public class CustomerOrderDao extends AbstractDao<CustomerOrder, Long> {

    public static final String TABLENAME = "CUSTOMER_ORDER";

    /**
     * Properties of entity CustomerOrder.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property ItemName = new Property(1, String.class, "itemName", false, "ITEM_NAME");
        public final static Property CustomerId = new Property(2, long.class, "customerId", false, "CUSTOMER_ID");
    }

    private Query<CustomerOrder> customer_CustomerOrderListQuery;

    public CustomerOrderDao(DaoConfig config) {
        super(config);
    }
    
    public CustomerOrderDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"CUSTOMER_ORDER\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"ITEM_NAME\" TEXT," + // 1: itemName
                "\"CUSTOMER_ID\" INTEGER NOT NULL );"); // 2: customerId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"CUSTOMER_ORDER\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, CustomerOrder entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String itemName = entity.getItemName();
        if (itemName != null) {
            stmt.bindString(2, itemName);
        }
        stmt.bindLong(3, entity.getCustomerId());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, CustomerOrder entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String itemName = entity.getItemName();
        if (itemName != null) {
            stmt.bindString(2, itemName);
        }
        stmt.bindLong(3, entity.getCustomerId());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public CustomerOrder readEntity(Cursor cursor, int offset) {
        CustomerOrder entity = new CustomerOrder( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // itemName
            cursor.getLong(offset + 2) // customerId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, CustomerOrder entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setItemName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setCustomerId(cursor.getLong(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(CustomerOrder entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(CustomerOrder entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(CustomerOrder entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "customerOrderList" to-many relationship of Customer. */
    public List<CustomerOrder> _queryCustomer_CustomerOrderList(long customerId) {
        synchronized (this) {
            if (customer_CustomerOrderListQuery == null) {
                QueryBuilder<CustomerOrder> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.CustomerId.eq(null));
                customer_CustomerOrderListQuery = queryBuilder.build();
            }
        }
        Query<CustomerOrder> query = customer_CustomerOrderListQuery.forCurrentThread();
        query.setParameter(0, customerId);
        return query.list();
    }

}
